//This file defines the basic configuration for the database (or local storage), so like It includes the database name, version, and tables.
export const dbConfig = {
   dbName: "HealthAssistantDB",
   version: 1,
   tables: ["healthData"],
 };
